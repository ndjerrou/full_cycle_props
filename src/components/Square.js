const Square = props => {
  console.log(props);
  return (
    <div
      style={{
        width: '60px',
        height: '60px',
        backgroundColor: 'black',
      }}
    >
      hi from Square App
    </div>
  );
};

export default Square;
